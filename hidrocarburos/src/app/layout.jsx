export default function RootLayout({ children }) {
  return (
  <html>
    <head>
      <title className="text-xl text-red-500">hidrocarburos</title>
    </head>
    <body>{children}</body>
  </html>
  )
}